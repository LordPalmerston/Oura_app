import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elmoahorinta.crackedoura',
  appName: 'Cracked Oura',
  webDir: 'dist'
};

export default config;
